# 검증 보고서

검증일: 2026-09-14 (Asia/Seoul). 아래 로컬 결과는 실제 실행한 명령과 저장된 산출물을 기준으로 작성했다. 원격 CI와 공개 배포 결과는 마지막 절에서 별도로 구분한다.

## 실행 환경

- Windows 11 Pro 23H2, x64 (`10.0.22631`).
- AMD Ryzen 5 5600G, 6코어/12스레드, 설치 메모리 16GB(프로세스에서 확인한 총량 15.38GiB).
- Node.js 24.13.1, npm lockfile 설치, TypeScript strict, Vitest 5.0.0, Playwright 1.63.0.
- 로컬 브라우저: 설치되어 있던 Google Chrome **152.0.7977.84**. Chromium 프로젝트의 `executablePath`로 실행했다.
- 로컬에 새 브라우저를 다운로드하지 않았다. 기존 Playwright Chromium 캐시는 실행 오류가 있었고, WebKit 캐시는 없어 WebKit은 원격 CI 대상이다.
- 데스크톱 CSS 뷰포트 1440×1000, 모바일 390×844 및 회전 844×390. 모바일은 브라우저 에뮬레이션이며 실제 휴대전화에서 수행한 검증이 아니다.
- 자원 사용이 큰 로컬 테스트는 하나씩 실행했다.

## 실제 명령과 결과

| 명령 | 결과 |
| --- | --- |
| `npm run typecheck` | 통과 |
| `npm run lint` | 통과 |
| `npm test` | 1차 122/122 통과. 추가 회귀 테스트를 포함한 최종 실행은 아래 coverage 명령 사용 |
| `npm run test:coverage` | **147/147 테스트, 11개 파일 통과, 9.51초** |
| `npm test -- tests/storage.test.ts -t QuotaExceededError` | 추가 저장 용량 오류 회귀 **1/1 통과**, 나머지 23개는 선택 실행으로 제외, 471ms |
| `npm run simulate` | 지도 **50/50**, 고정 AI 대전 **20/20**, 오류 0, 총 84.76초 |
| `npm run build` | strict 타입 검사 및 Vite production build 통과, 32개 모듈 |
| `npm run test:e2e -- --project=chromium-desktop --project=chromium-mobile` | **12/12 통과, 52.7초** |
| `npm run test:e2e -- --project=chromium-mobile --grep 'two-finger pinch'` | 추가 실제 두 손가락 입력 회귀 **1/1 통과, 10.1초** |
| `npm run test:subpath` | `/civilization-clone/`에서 HTML/에셋 HTTP 200, 새 게임·도시 건설·저장 후 새로고침 복원 통과 |
| `npm run measure:build` | gzip 압축 합계 5MB 목표 이내. 파일별 최신 수치는 `artifacts/build-metrics.json` 참조 |

로컬 브라우저 재현:

```powershell
$env:PLAYWRIGHT_CHROMIUM_EXECUTABLE='C:\Program Files\Google\Chrome\Application\chrome.exe'
npm run build
npm run test:e2e -- --project=chromium-desktop --project=chromium-mobile
```

Playwright는 실제 production 빌드를 `vite preview`로 실행한다. 브라우저 경로는 각 환경에 설치된 실행 파일로 바꾸거나, 원격 CI에서 제공하는 Playwright 브라우저를 사용한다.

## 단위·통합 테스트 범위와 커버리지

| 범위 | 분기 커버리지 |
| --- | ---: |
| 측정한 엔진·저장 전체 | **86.17% (1066/1237)** |
| 공통 명령 엔진 `engine.ts` | **89.83%** |
| Utility AI | **90.70%** |
| 경제 | **95.74%** |
| 전투 | **88.40%** |
| 이동 | **87.09%** |
| 승패 | **95.34%** |
| 지도·시야 | **85.29%** |
| 저장 | **76.71%** |

전체 문장 91.12%, 함수 95.08%, 라인 98.05%. 정적 balance 데이터와 타입 선언, barrel 파일은 커버리지 측정에서 제외했다. 커버리지는 실행한 분기의 비율이며 모든 조합의 정확성을 보장하는 수치가 아니다. 요약은 `artifacts/coverage-summary.json`, 재생성한 상세 HTML은 `coverage/index.html`이다.

위 커버리지는 QuotaExceededError 회귀와 저장 실패 시 복구본 회전까지 취소하는 처리를 포함한 최종 147개 전체 테스트의 측정값이다. 공간 부족 오류 후 원래 저장뿐 아니라 복구본도 변경되지 않음을 확인했다.

검증 내용:

- 육각 거리·경계·최단 경로, 지형 이동 비용, 이동력 소진, 전투/비전투 중첩, 평화 중 무단 진입 차단.
- 동일 시드/RNG 재현, 지도 연결성 및 시작 산출·거리, 시야 밖 유닛·자원·도시 내부 숨김, 마지막 확인 도시 정보.
- 개척자 소모·수도 식별·도시 거리, 중복 노동 방지와 잠금 경쟁, 성장·기아 하한·유지비·부채 방지.
- 생산/연구 변경 시 진척 보존, 초과분 이월, 생산 배치 대기, 다음 자기 턴부터 생산 유닛 행동, 개척자 인구 비용.
- 기술 선행 DAG, 실제 산출·전투 보너스, 전략 자원 해금, 과학 집중의 생산 전환, 일꾼 작업 횟수와 중복 개량 방지.
- 근접/원거리 예상 피해와 실제 피해 일치, 방어·체력·기술, 동시 전사, 비전투 유닛, 주둔 병력 우선 공격, 체력 0 도시의 근접 점령과 점령 상태 갱신.
- 선전포고 확인·접촉·강화 유지·무단 주둔 해소, 원자적 금 거래, 중복 교역 차단, 정책 교체 비용, 외교 관계의 저장 가능 범위 유지.
- 정복·과학·점수·패배, 살아 있는 미정착 세력의 정복 유보, 정착 전 탈락, 동시 승리 우선순위·점수 동률, 중복 턴 종료·중복 정산 방지.
- AI의 관측 정보만 사용하는 동일 행동, 행동 상한, 안전한 턴 종료, 도시 확장·연구·전투·완주 회귀.
- JSON/IndexedDB 왕복, 3개 수동 슬롯, 자동 저장 직렬화·즉시 스냅샷·최근 정상 복구본 2개, 저장 차단 뒤 파일 내보내기 및 복구.
- fake IndexedDB의 실제 `IDBObjectStore.put` 호출 지점에서 `QuotaExceededError`를 주입하여 저장 실패 전달·기존 정상 저장 유지·메모리의 JSON 내보내기·후속 명령과 저장 복구를 확인했다. 실제 디스크 용량을 소진시키지는 않았다.
- 손상 JSON·5MB 초과·지원하지 않는 버전·NaN·잘못된 참조·중복 엔티티·잘못된 연구·손상 주 저장본·손상 첫 복구본 거부/복구.
- 정지할 수 있는 죽은 AI 행동 세력, 중복 정산 페이즈, AI 경로 탐색을 깨뜨릴 수 있는 미탐험 도시 기억을 가져오기 단계에서 거부.
- 런타임 명령의 `constructor`/`toString` 생산 ID를 거부하여 데이터 표의 프로토타입 속성으로 잘못된 생산이 들어오지 않음.

추가 독립 검토에서 `ai-match-01`의 정상 명령/AI/정산 경계 **200개**와 최종 과학 승리(40라운드), 실제 점령 및 상대 탈락 직후 상태를 모두 `validateSave`로 확인했다.

## 지도 50개와 AI 대전 20개

대표 결과 파일: **`artifacts/simulation-results.json`**. 최초 밸런스에서 전투가 없었던 이전 결과는 `artifacts/simulation-before-balance.json`으로 보존했으며 최종 수치에 혼합하지 않는다.

지도 시드는 `map-audit-01`~`map-audit-50`, AI 시드는 `ai-match-01`~`ai-match-20`으로 고정했다. 인간 0번 세력에도 같은 관측 기반 AI와 합법 명령 처리를 사용했다. 각 세력 턴/정산 경계에서 엔진 검사와 독립 검사를 함께 수행했다. 첫 라운드의 4개 AI/정산 단계별 세이브 왕복 및 다음 상태 일치, 최종 저장 검증도 포함했다.

| 측정 항목 | 최종 결과 |
| --- | ---: |
| 유효한 지도 | 50 / 50 |
| 정상 종료한 대전 | 20 / 20 |
| NaN·참조·중복 엔티티·중복 노동·중첩·정산·진행 정지 오류 | 0 |
| 승리 유형 | 과학 20, 정복 0, 점수 0 |
| 종료 라운드 | 최소 37 / 평균 **38.8** / 최대 40 |
| 종료 시 도시 수 | 9~13 |
| 실제 `ATTACK` 명령 | **352회**, 각 경기 6~31회 |
| 도시 점령 / 유닛 처치 | **9 / 95** |
| 승리 세력별 경기 수 | 새잎 1 / 붉은모루 3 / 별샘 9 / 황금돛 7 |
| 경기 전체 처리 시간 | 평균 **4.23초**, 최대 **4.80초** |
| 한 라운드 AI 3세력 행동+정산 최대 | **168.11ms** |
| 50개 지도+20개 경기 전체 검사 | **84.76초** |

기술 수, 명령 순번, 정산 라운드, 점수, 경기별 전투·점령 및 처리 시간은 JSON에 저장했다. `attacks`는 실제 공격 횟수다. 별도 `combatNotifications`는 공격 통지와 점령 통지를 포함하므로 공격 횟수와 같은 뜻으로 사용하지 않는다.

AI 처리 2초 목표는 위 Windows/Node headless 참조 환경에서 충족했다. 이 수치를 브라우저 렌더링·저장·애니메이션을 모두 합친 시간이나 저사양 휴대전화 성능으로 일반화하지 않는다. 모든 자동 경기는 과학 승리로 끝났으므로 정복/점수 승리의 자연 발생 빈도와 장기 밸런스는 추가 플레이가 필요하다. 정복과 점수 규칙 자체는 실제 공격·라운드 정산을 거치는 단위 및 브라우저 테스트로 검증했다.

## 브라우저 실플레이와 스크린샷

각 Chromium 프로젝트에서 동일한 6개 시나리오를 통과했다.

1. 새 게임 → 도시 건설 → 생산/연구 선택 → 지도 이동 → 4라운드 → 수동 저장 → JSON 내보내기 → 새로고침 복원 → 추가 라운드 → JSON 가져오기. 전체 상태가 저장 당시와 일치함을 확인했다.
2. 준비 상태의 마지막 적 수도를 지도에서 공격하고 실제 엔진 명령으로 점령 → 실제 턴 정산 → 정복 결과 → 새 게임 재시작.
3. 합법적으로 선택한 대학술원 프로젝트의 남은 생산을 실제 라운드로 투입 → 과학 결과.
4. 마지막 설정 라운드를 실제로 정산 → 점수 결과.
5. 손상 세이브 가져오기 실패 후 기존 상태 유지, 주요 컨트롤의 가로 잘림 검사, 연구 패널.
6. 지도 확대 후 실제 마우스 클릭/모바일 터치로 예상 타일 이동, 화면 크기/방향 변경 후 다시 정확한 타일 이동.

추가 Chromium 모바일 테스트는 CDP `Input.dispatchTouchEvent`의 두 접점 start/move/end를 전송해 **105% → 240% 핀치 확대**를 확인했다. 핀치 자체가 게임 명령을 발생시키지 않고, 확대 후 타일을 터치하면 정확히 인접 칸으로 한 번 이동함을 저장 상태로 확인했다. WebKit에는 CDP 세션이 없어 이 추가 테스트만 명시적으로 제외하며, 데스크톱 프로젝트에서도 제외한다.

준비 상태는 production 앱의 JSON 가져오기와 실제 저장 검증을 통과시켜 넣었다. 결과 필드를 미리 만들어 가짜 승리 화면만 띄우는 방식은 사용하지 않았다. 핵심 실플레이에서 콘솔 오류 및 페이지 오류 0건. 390px 가로 넘침 없음. 실제 모바일 기기 및 저장 공간을 실제로 소진시키는 검증은 미실행이다.

`artifacts/screenshots/`에 실제 실행 PNG를 보관한다.

- `chromium-desktop-menu.png`, `chromium-mobile-menu.png`
- `chromium-desktop-empire-round-4.png`, `chromium-mobile-empire-round-4.png`
- 각 프로젝트의 `combat-preview`, `conquest-result`, `science-result`, `score-result`, `research-panel`, `camera-rotated`
- `chromium-mobile-pinch-zoom.png`: 실제 두 손가락 입력 뒤 240% 확대 및 이동
- `subpath-game.png`: production 하위 경로 검증

상세 브라우저 실행 보고서는 `artifacts/playwright-report/index.html`로 재생성된다. 스크린샷은 에뮬레이션 DPR 때문에 CSS 뷰포트보다 큰 픽셀 크기일 수 있다.

## 원격 CI와 배포 검증

WebKit 모바일 검증은 로컬에서 실행하지 않았다. 첫 원격 CI의 커밋 `86656c50`에서는 Chromium 데스크톱 5개·Chromium 모바일 5개·WebKit 모바일 5개, **15/15** 브라우저 테스트가 실제 통과했다. [확인한 Actions 실행](https://github.com/orac83223-tech/civilization-clone/actions/runs/34769049649). 이는 이후 추가된 카메라 회전·핀치 및 마지막 저장 회귀를 포함하지 않는 첫 체크포인트 결과다. 후속 커밋의 전체 CI는 [PR Checks](https://github.com/orac83223-tech/civilization-clone/pull/1/checks)에서 실제 SHA별로 확인하며, 최종 응답에서 확인한 최신 실행을 함께 제공한다.

하위 경로 검증은 로컬 정적 production 서버에서 수행한 결과이며 공개 배포를 의미하지 않는다. 실제 공개 플레이 URL과 GitHub Pages 권한/요금제 제한은 `DEPLOYMENT.md`를 기준으로 확인한다. 사람의 30~60분 플레이 시간, 실제 기기 조작성·발열, 숙련자 재미/난이도 및 과학 외 자연 승리 분포는 아직 측정하지 않았다.
