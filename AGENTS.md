# Codex 작업 지침

- README.md와 docs/DEPLOYMENT.md를 읽고 실제 작업 환경, 브랜치, git status를 확인한다.
- 현재 저장소는 사전 설정과 실행 가능한 시작 화면만 포함한다. 게임 완성으로 보고하지 않는다.
- 사용자 변경을 보존하고 기능별 codex/ 브랜치에서 개발한다. 완료한 코드는 커밋·push하고 PR로 남긴다.
- 원격 SHA와 CI 결과를 확인하고 실행하지 못한 검사는 명시한다.
- 비밀키, 인증서, 환경변수 전체를 출력하거나 커밋하지 않는다.
- 대용량 의존성·SDK 설치와 브라우저 다운로드는 로컬에서 자동 실행하지 않는다. 원격 CI를 사용한다.
- 독자적인 게임 이름·코드·아트·음악을 사용한다. 기존 상용 게임의 에셋을 복제하지 않는다.
- 엔진, 데이터, UI를 분리하고 저장 형식에는 버전을 둔다. 핵심 규칙과 저장은 실제 동작으로 검증한다.
- 이번 범위는 비공개 GitHub 업로드와 배포 준비다. 실제 공개·App Store 제출은 후속 배포 요청에서 진행한다.

## 문명클론 웹게임
- 기본은 의존성 없는 JavaScript 정적 웹 시작점이다. 게임 규모에 맞춰 TypeScript/Vite로 전환 가능하다.
- src/engine, src/data, src/ui로 확장한다. 시드 기반 지도·턴 전이·자원·승리 조건을 순수 함수로 구성한다.
- `npm ci`, `npm run check`, `npm run build`; 원격 개발은 `npm run dev`.
- dist/가 배포 결과이며 상대 경로로 GitHub Pages 저장소 하위 경로를 지원한다.
