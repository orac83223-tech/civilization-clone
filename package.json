{
  "name": "civilization-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 0.0.0.0 --directory src",
    "check": "node --check src/main.js && node --check scripts/build.mjs",
    "build": "node scripts/build.mjs"
  }
}
